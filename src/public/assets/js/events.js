'use strict';
const body=document.body;const userId=body.dataset.userId;if(userId){const key=`avance.events.v1.${userId}`;let lastId=Number(localStorage.getItem(key)||0);let audioReady=false;document.addEventListener('pointerdown',()=>{audioReady=true;},{once:true});const sound=(type)=>{if(!audioReady)return;const AudioContext=window.AudioContext||window.webkitAudioContext;if(!AudioContext)return;const ctx=new AudioContext(),osc=ctx.createOscillator(),gain=ctx.createGain();osc.frequency.value=type==='ranking_overtake'?740:type==='daily_goal_reached'?880:520;gain.gain.setValueAtTime(.08,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.8);osc.connect(gain).connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+.8);};const celebrate=(type)=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;body.classList.add(type==='ranking_overtake'?'flash':'celebrate');setTimeout(()=>body.classList.remove('flash','celebrate'),1200);};const consume=(event)=>{const id=Number(event.lastEventId);if(!id||id<=lastId)return;lastId=id;localStorage.setItem(key,String(id));sound(event.type);if(event.type!=='sale_created')celebrate(event.type);};let polling=false;const poll=async()=>{try{const response=await fetch(`/api/v1/dashboard/events/poll?after=${lastId}`,{credentials:'same-origin'}),json=await response.json();for(const row of json.data||[])consume({lastEventId:String(row.id),type:row.type});}catch{}if(polling)setTimeout(poll,5000);};if(window.EventSource){const source=new EventSource(`/api/v1/dashboard/events?after=${lastId}`);for(const type of['sale_created','ranking_overtake','daily_goal_reached'])source.addEventListener(type,consume);source.onerror=()=>{source.close();polling=true;poll();};}else{polling=true;poll();}}
